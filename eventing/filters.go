package eventing

// EventClassesFilters is used to filter classes participating in an event
type EventClassesFilters struct {
	Name string
}

// EventPupilsFilters is used to filter pupils participating in an event
type EventPupilsFilters struct {
	Name string
}
